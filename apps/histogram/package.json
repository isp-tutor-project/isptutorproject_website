{
  "name": "@isptutorproject/histogram",
  "version": "0.1.0",
  "description": "histogram lesson",
  "main": "src/index.js",
  "license": "Proprietary",
  "private": true,
  "scripts": {
    "build": "rimraf dist && NODE_ENV=production DEBUG=false webpack --mode=development",
    "watch": "rimraf dist && NODE_ENV=development DEBUG=true webpack --mode=production"
  },
  "dependencies": {
    "@isptutorproject/navbar": "0.1.0"
  },
  "devDependencies": {
    "@isptutorproject/webpack-config": "0.1.0",
    "rimraf": "^3.0.2",
    "webpack": "^4.44.2",
    "webpack-cli": "^3.3.12"
  }
}
