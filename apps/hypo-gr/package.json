{
  "name": "@isptutorproject/hypo-gr",
  "version": "0.1.0",
  "description": "hypothesis GR module",
  "main": "src/index.js",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "rimraf dist && node scripts/buildData.js && NODE_ENV=production DEBUG=false webpack --mode=production && eleventy",
    "watch": "rimraf dist && node scripts/buildData.js && NODE_ENV=development DEBUG=true webpack --watch --mode=development & eleventy --watch"
  },
  "dependencies": {
    "@isptutorproject/isp-database": "0.1.0",
    "@isptutorproject/isp-site-styles": "0.1.0",
    "@isptutorproject/snackbar": "0.1.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "0.11.0",
    "@isptutorproject/eleventy-config": "0.1.0",
    "@isptutorproject/webpack-config": "0.1.0",
    "rimraf": "^3.0.2",
    "sass": "^1.26.8",
    "sass-loader": "^8.0.2",
    "css-loader": "^3.5.0",
    "webpack": "^4.43.0",
    "webpack-cli": "3.3.11"
  }
}
