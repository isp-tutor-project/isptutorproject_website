{
  "name": "@isptutorproject/isp-api",
  "version": "0.1.0",
  "description": "lib which is a wrapper around isp-database and activity-config",
  "module": "src/index.js",
  "license": "Proprietary",
  "private": true,
  "scripts": {
    "build": "rimraf dist && NODE_ENV=production DEBUG=false webpack --mode=production",
    "watch": "rimraf dist && NODE_ENV=development DEBUG=true webpack --watch --mode=development"
  },
  "dependencies": {
    "@isptutorproject/activity-config": "0.1.0",
    "@isptutorproject/isp-database": "0.1.0"
  },
  "devDependencies": {
    "@isptutorproject/webpack-config": "0.1.0",
    "rimraf": "^3.0.2",
    "webpack": "4.42.0",
    "webpack-cli": "3.3.11"
  }
}
